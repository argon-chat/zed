[package]
name = "ztracing"
version = "0.1.0"
edition.workspace = true
publish.workspace = true
license = "Apache-2.0"

[lints]
workspace = true

[features]
# Accepted for manifest compatibility with crates that enable them; the
# profiling integrations are intentionally absent from this shim.
tracy = []
web = []

[dependencies]
tracing.workspace = true
ztracing_macro.workspace = true
